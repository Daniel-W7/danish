#!/bin/sh

prefix=@prefix@
exec_prefix=@exec_prefix@

if [ "$DISPLAY " != " " ] && [ -f @bindir@/danioc ]; then
	exec @bindir@/danioc ${1+"$@"}
elif [ -f @bindir@/danioc ]; then
	exec @bindir@/danioc ${1+"$@"}
else
	echo "Error: Can't find gFTP binaries installed in @bindir@"
fi

